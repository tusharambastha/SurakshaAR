/**
 * SurakshaAR — Fire Detection AR Overlay (DISABLED)
 *
 * Real fire HUD, flame reticle, confidence indicator, and real flame auto-advance
 * have been completely removed.
 */

export default function FireDetectionOverlay() {
  return null
}
