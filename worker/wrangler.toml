name = "suraksha-chat-worker"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets required (set via CLI, never commit here):
# npx wrangler secret put GEMINI_API_KEY

[vars]
ENVIRONMENT = "production"
